{
  "status": "ready",
  "loop_name": "ralph-loop-502",
  "todos": [
    {
      "id": "502-1",
      "content": "Read CLAUDE.md and update: (1) replace 'Token is read with python3 -c' convention with graph_call.py proxy pattern description, (2) update Setup section to reference outlook-skills/auth.sh instead of .env and outlook-auth-server.js, (3) add a Security Architecture section describing encrypted tokens at rest, OS keychain, and graph_call.py as the only token interface, (4) remove ~/.outlook-mcp-tokens.json from all references, (5) update Environment Variables table to remove OUTLOOK_CLIENT_SECRET (now in keychain)",
      "skill": "NA",
      "agent": "worker",
      "outcome": "CLAUDE.md references graph_call.py as the API call mechanism; contains Security Architecture section; zero references to ~/.outlook-mcp-tokens.json or 'Token is read with python3 -c'; Setup section references auth.sh",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-2",
      "content": "Read .gitignore and add two entries if not already present: 'outlook-skills/.venv/' and '*.enc'",
      "skill": "NA",
      "agent": "worker",
      "outcome": ".gitignore contains outlook-skills/.venv/ and *.enc entries",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-3",
      "content": "Read package.json and verify test:security script is present (added in loop 500); also check that the existing test scripts still reference the correct test file paths; add a note comment in the scripts section if helpful",
      "skill": "NA",
      "agent": "worker",
      "outcome": "package.json test:security script is present; all test script paths are correct and functional",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-4",
      "content": "Read scripts/outlook-auth-server.js and add a DEPRECATED header comment at the very top of the file (before any existing code): '// DEPRECATED: This Node.js OAuth server has been replaced by outlook-skills/auth.sh (Python). // This file is retained for reference only. Do not use in new workflows.'",
      "skill": "NA",
      "agent": "worker",
      "outcome": "scripts/outlook-auth-server.js first lines contain the DEPRECATED comment; file functionality is otherwise unchanged",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-5",
      "content": "Read scripts/outlook-token-refresh.js and add the same DEPRECATED header comment at the top: '// DEPRECATED: Token refresh is now handled internally by outlook-skills/token_helper.py via graph_call.py. // This file is retained for reference only. Do not use in new workflows.'",
      "skill": "NA",
      "agent": "worker",
      "outcome": "scripts/outlook-token-refresh.js first lines contain the DEPRECATED comment; file functionality is otherwise unchanged",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-6",
      "content": "Run the full test suite: npm test (runs static + unit + eval). Report pass/fail counts. If anything fails that was passing before this phase, investigate and fix.",
      "skill": "NA",
      "agent": "worker",
      "outcome": "npm test exits 0; all static, unit, and eval suites pass; no regressions from Phase 5 changes",
      "status": "pending",
      "priority": "high"
    },
    {
      "id": "502-7",
      "content": "Final security scan: grep -r 'outlook-mcp-tokens.json' . --include='*.md' --include='*.js' --include='*.py' --include='*.sh' (excluding node_modules and .git); grep -r 'Authorization: Bearer' . --include='*.md' (excluding node_modules). Report any matches found outside of deprecated scripts and test fixtures.",
      "skill": "NA",
      "agent": "worker",
      "outcome": "Zero outlook-mcp-tokens.json references in non-deprecated files; zero 'Authorization: Bearer' in any SKILL.md; any matches found are in deprecated scripts only (acceptable)",
      "status": "pending",
      "priority": "high"
    }
  ],
  "source_context": {
    "claude_md_sections_to_update": [
      {
        "section": "Architecture paragraph (line 9)",
        "old_text": "Instead of an MCP server, this project uses **skill files** that teach Claude how to call the Microsoft Graph API directly using `curl`. The only Node.js components are lightweight scripts for OAuth authentication (which requires a local callback server).",
        "what_it_should_become": "Replace 'directly using `curl`' with 'via `python3 scripts/graph_call.py`'. Update Node.js component description to acknowledge the scripts are now deprecated and the Python-based auth.sh / token_helper.py is the current approach."
      },
      {
        "section": "Architecture directory tree — scripts block (lines 23-25)",
        "old_text": "scripts/\n  outlook-auth-server.js     — OAuth callback server (port 3333)\n  outlook-token-refresh.js   — Token refresh script",
        "what_it_should_become": "Add graph_call.py as the primary script. Mark outlook-auth-server.js and outlook-token-refresh.js as (deprecated)."
      },
      {
        "section": "Commands block (lines 41, 43-44)",
        "old_text": "npm run test:eval    # skill selection / curl pattern eval\nnode scripts/outlook-auth-server.js   # start OAuth callback server manually\nnode scripts/outlook-token-refresh.js # refresh token manually",
        "what_it_should_become": "Update test:eval description to reference graph_call.py pattern eval. Mark the two node script commands as deprecated or remove them. Add note about test:security."
      },
      {
        "section": "Setup section (lines 49-52)",
        "old_text": "1. Copy `.env.example` to `.env`\n2. Set `OUTLOOK_CLIENT_ID` and `OUTLOOK_CLIENT_SECRET` from your Azure AD app registration\n3. Run `npm install`\n4. Use `/outlook-auth` to authenticate — tokens are saved to `~/.outlook-mcp-tokens.json`",
        "what_it_should_become": "Update to reference outlook-skills/auth.sh instead of .env and outlook-auth-server.js. Remove '~/.outlook-mcp-tokens.json' reference. Note that OUTLOOK_CLIENT_SECRET is stored in the OS keychain, not .env."
      },
      {
        "section": "Key Conventions block (lines 75-81)",
        "old_text": "- All API calls use `curl` with OAuth tokens from `~/.outlook-mcp-tokens.json`\n- Token is read with `python3 -c \"import json; print(json.load(open(...))['access_token'])\"`",
        "what_it_should_become": "Replace both lines: API calls use `python3 scripts/graph_call.py <METHOD> <endpoint>`. Token is never read directly — graph_call.py handles auth internally via token_helper.py."
      },
      {
        "section": "Environment Variables table (lines 107-113)",
        "old_text": "| `OUTLOOK_CLIENT_SECRET` | Yes | Azure AD application client secret |",
        "what_it_should_become": "Change Required from Yes to No (or remove the row) — OUTLOOK_CLIENT_SECRET is now stored in the OS keychain, not required as an env var in production."
      },
      {
        "section": "Missing: Security Architecture section",
        "old_text": "(does not exist)",
        "what_it_should_become": "Add a new '## Security Architecture' section between 'Key Conventions' and 'Available Skills' describing: encrypted token storage, OS keychain integration, graph_call.py as the sole token interface, and why the old Node.js scripts are deprecated."
      }
    ],
    "gitignore_missing_entries": {
      "outlook_skills_venv": {
        "entry": "outlook-skills/.venv/",
        "present": false,
        "note": "Not in .gitignore — needs to be added under the existing '# Folders' section or a new '# Python virtual environments' section"
      },
      "enc_files": {
        "entry": "*.enc",
        "present": false,
        "note": "Not in .gitignore — needs to be added under the '# Tokens and credentials' section alongside *.pem, *.key, *.cert, *.token.json"
      }
    },
    "scripts_need_deprecation": {
      "outlook_auth_server_js": {
        "file": "scripts/outlook-auth-server.js",
        "current_line_1": "#!/usr/bin/env node",
        "current_line_2": "/**",
        "current_line_3": " * Slim OAuth Authentication Server for Outlook",
        "has_deprecated_header": false,
        "action": "Insert DEPRECATED comment block before line 1 (or after the shebang on line 1)"
      },
      "outlook_token_refresh_js": {
        "file": "scripts/outlook-token-refresh.js",
        "current_line_1": "#!/usr/bin/env node",
        "current_line_2": "/**",
        "current_line_3": " * Outlook Token Refresh Script",
        "current_line_4": " * Reads refresh_token from ~/.outlook-mcp-tokens.json,",
        "has_deprecated_header": false,
        "action": "Insert DEPRECATED comment block before line 1 (or after the shebang on line 1). Note: line 4 also contains ~/.outlook-mcp-tokens.json — the deprecation comment makes this acceptable as it describes legacy behavior."
      }
    },
    "curl_structure_test_status": {
      "file": "test/eval/curl-structure.test.js",
      "status": "FAILING — 12 failures out of 97 tests in npm run test:eval",
      "failure_source": "All 12 failures are from curl-structure.test.js; the failures are in skill suites that look for raw curl patterns (e.g., 'should have GET me/messages') which no longer exist after the graph_call.py migration",
      "failing_skills": [
        "outlook-email-list",
        "outlook-email-read",
        "outlook-email-send",
        "outlook-email-reply",
        "outlook-calendar-list",
        "outlook-calendar-respond"
      ],
      "action_needed": "curl-structure.test.js must be removed or disabled in loop 502. The replacement (api-call-structure.test.js) was created in loop 500 and passes. Removing curl-structure.test.js will eliminate the 12 failures and allow npm run test:eval (and npm test) to exit 0.",
      "note": "todo 502-6 requires npm test exits 0 — this is currently blocked by curl-structure.test.js failures. Removing it is required."
    },
    "package_json_status": {
      "test_security_present": true,
      "test_security_value": "node --test test/static/security.test.js",
      "all_scripts": {
        "auth-server": "node scripts/outlook-auth-server.js",
        "token-refresh": "node scripts/outlook-token-refresh.js",
        "test": "node --test test/static/*.test.js test/unit/*.test.js test/eval/*.test.js test/integration/*.test.js",
        "test:static": "node --test test/static/*.test.js",
        "test:security": "node --test test/static/security.test.js",
        "test:unit": "node --test test/unit/*.test.js",
        "test:eval": "node --test test/eval/*.test.js",
        "test:integration": "OUTLOOK_INTEGRATION_TEST=true node --test test/integration/*.test.js"
      },
      "stale_references": [
        "auth-server and token-refresh scripts reference deprecated Node.js scripts but are themselves just convenience scripts — acceptable to keep or annotate",
        "No stale test file paths — all glob patterns are correct"
      ],
      "action_needed": "Verify only — test:security is already present and correct. No changes required to package.json in todo 502-3."
    }
  },
  "handoff_injected": {
    "source": "git log + phase-5-ralph-loops.md loop-501 handoff_summary + .claude/state/loop-ready.json (loop-501)",
    "loop_name": "ralph-loop-501",
    "status": "completed",
    "commit": "3f870b5",
    "commit_message": "complete: ralph-loop-501 - unit tests annotated deprecated, smoke.test.js rewritten for graph_call.py",
    "handoff": {
      "done": "Annotated auth-server.test.js and token-refresh.test.js as deprecated; rewrote smoke.test.js to use graph_call.py with no token file reads; npm run test:unit exits 0.",
      "failed": "",
      "needed": "Loop 502 should update CLAUDE.md security architecture, .gitignore, deprecate old Node.js scripts, and run final verification."
    }
  }
}

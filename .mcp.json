{
  "mcpServers": {
    "outlook": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/src/index.js"],
      "env": {
        "OUTLOOK_TOKEN_FILE": "${CLAUDE_PLUGIN_ROOT}/outlook-skills/tokens.json"
      }
    }
  }
}
